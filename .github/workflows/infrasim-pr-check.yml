name: ChaosProof Infrastructure Check

on:
  pull_request:
    paths:
      - '**/*.yaml'
      - '**/*.yml'
      - '**/terraform/**'
      - '**/*.tf'
      - '**/infrasim*.yaml'
      - '**/chaosproof*.yaml'

permissions:
  pull-requests: write
  contents: read

jobs:
  chaosproof-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install ChaosProof
        run: pip install chaosproof  # or pip install -e . if in same repo

      - name: Find infrastructure YAML files
        id: find-yaml
        run: |
          # Find ChaosProof/InfraSim YAML files in the PR
          FILES=$(find . -name 'chaosproof*.yaml' -o -name 'infrasim*.yaml' -o -name 'chaosproof*.yml' -o -name 'infrasim*.yml' -o -name 'infrastructure*.yaml' | head -5)
          if [ -z "$FILES" ]; then
            echo "No ChaosProof YAML files found"
            echo "found=false" >> $GITHUB_OUTPUT
          else
            echo "found=true" >> $GITHUB_OUTPUT
            echo "files=$FILES" >> $GITHUB_OUTPUT
            echo "first_file=$(echo $FILES | head -1)" >> $GITHUB_OUTPUT
          fi

      - name: Run ChaosProof simulation
        if: steps.find-yaml.outputs.found == 'true'
        run: |
          FILE="${{ steps.find-yaml.outputs.first_file }}"
          echo "Running ChaosProof on $FILE..."

          # Run simulation with JSON output
          chaosproof load "$FILE"
          chaosproof simulate --json chaosproof-results.json 2>&1 | tee chaosproof-output.txt

          # Run AI analysis
          chaosproof analyze "$FILE" --json 2>&1 | tee chaosproof-analysis.txt

      - name: Comment PR with results
        if: steps.find-yaml.outputs.found == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            let output = '';
            try { output = fs.readFileSync('chaosproof-output.txt', 'utf8'); } catch(e) {}
            let analysis = '';
            try { analysis = fs.readFileSync('chaosproof-analysis.txt', 'utf8'); } catch(e) {}

            // Parse key metrics from output
            const lines = output.split('\n');
            const resilienceLine = lines.find(l => l.includes('Resilience'));
            const criticalLine = lines.find(l => l.includes('CRITICAL') || l.includes('Critical'));

            const body = `## ChaosProof Chaos Simulation Report

            ${resilienceLine ? '**' + resilienceLine.trim() + '**' : ''}
            ${criticalLine ? criticalLine.trim() : ''}

            <details>
            <summary>Full Simulation Output</summary>

            \`\`\`
            ${output.substring(0, 3000)}
            \`\`\`
            </details>

            <details>
            <summary>AI Analysis & Recommendations</summary>

            \`\`\`
            ${analysis.substring(0, 3000)}
            \`\`\`
            </details>

            ---
            *Powered by [ChaosProof](https://github.com/mattyopon/infrasim) -- Zero-Risk Infrastructure Chaos Simulation*`;

            // Find existing ChaosProof comment to update
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existingComment = comments.find(c =>
              c.body.includes('ChaosProof Chaos Simulation Report'));

            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body,
              });
            }
